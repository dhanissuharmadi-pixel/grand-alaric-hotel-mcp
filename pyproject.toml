[project]
name = "hotel-mcp-server"
version = "0.1.0"
description = "MCP server for Grand Alaric Hotel booking (Bandung, Indonesia)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.27.2",
    "openai-agents>=0.17.5",
    "python-dotenv>=1.2.2",
]
