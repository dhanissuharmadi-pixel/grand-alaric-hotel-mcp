[project]
name = "hotel-mcp-server"
version = "0.1.0"
description = "White-label hotel booking MCP server + ChatGPT Apps SDK widgets"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.27.2",
    "python-dotenv>=1.2.2",
]
